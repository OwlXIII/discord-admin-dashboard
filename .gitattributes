# Normalize line endings so the repo is consistent across Windows/Linux/Mac.
* text=auto

# The Maven wrapper scripts must keep the right line endings and stay executable.
mvnw       text eol=lf
*.sh       text eol=lf
mvnw.cmd   text eol=crlf
*.bat      text eol=crlf
*.cmd      text eol=crlf
